// Reverse an array.
lambda reverse = array x -> {
	int length = size(x)
	array y = new(length)
	int i = 1
	while i <= length {
		y[i - 1] = x[length - i]
		i = i + 1
	}
	return y
}

import("lib/Dump.scandal")
array reverseTest = reverse.dump([1, 2, 3, 4, 5])
